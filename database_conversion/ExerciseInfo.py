class ExerciseInfo:
    def __init__(self, exercise_id, side, page_and_exercise, tenor, treble, book_id):
        self.exercise_id = exercise_id
        self.side = side
        self.page_and_exercise = page_and_exercise
        self.tenor = tenor
        self.treble = treble
        self.book_id = book_id
